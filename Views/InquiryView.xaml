<UserControl x:Class="SarasaviLibrary.Views.InquiryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Search Section -->
        <materialDesign:Card Grid.Row="0" Margin="8" Padding="16">
            <StackPanel>
                <TextBlock Text="Search Books" FontSize="20" FontWeight="Medium" Margin="0 0 0 16"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtSearchQuery" Grid.Column="0" materialDesign:HintAssist.Hint="Search by Title, Author(not implemented), or Book Number" Style="{StaticResource MaterialDesignFloatingHintTextBox}" VerticalAlignment="Center" Margin="0 0 16 0"/>
                    <Button Grid.Column="1" Content="Search" Width="100" Click="BtnSearch_Click" />
                </Grid>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- Results Section -->
        <materialDesign:Card Grid.Row="1" Margin="8" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Search Results &amp; Availability" FontSize="18" FontWeight="Medium" Margin="0 0 0 16" Grid.Row="0"/>
                
                <DataGrid x:Name="DgResults" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Book Number" Binding="{Binding Book.BookNumber}" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Book.Title}" Width="*" />
                        <DataGridTextColumn Header="Copy Number" Binding="{Binding FullCopyNumber}" />
                        <DataGridTextColumn Header="Reference Only" Binding="{Binding IsReferenceOnly}" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
